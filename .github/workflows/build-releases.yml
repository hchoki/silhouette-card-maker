name: Build Releases

on:
  push:
    tags:
      - 'v*'  # Trigger on version tags like v1.0.0
  workflow_dispatch:  # Allow manual trigger

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pyinstaller
      
      - name: Build Windows executable
        run: |
          cd build_tools && python -m PyInstaller --clean --distpath .. --workpath build SilhouetteCardMaker.spec
      
      - name: Get version
        id: get_version
        shell: bash
        run: |
          if [[ $GITHUB_REF == refs/tags/* ]]; then
            echo "version=${GITHUB_REF#refs/tags/}" >> $GITHUB_OUTPUT
          else
            echo "version=dev-$(date +'%Y%m%d')" >> $GITHUB_OUTPUT
          fi
      
      - name: Package Windows release
        shell: bash
        run: |
          mkdir -p release
          cp SilhouetteCardMaker.exe release/
          cp -r assets calibration cutting_templates plugins game examples release/
          cp README.md LICENSE.md requirements.txt release/
          cd release
          7z a -tzip ../SilhouetteCardMaker-${{ steps.get_version.outputs.version }}-Windows.zip *
      
      - name: Upload Windows artifact
        uses: actions/upload-artifact@v4
        with:
          name: SilhouetteCardMaker-Windows
          path: SilhouetteCardMaker-${{ steps.get_version.outputs.version }}-Windows.zip

  build-macos:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pyinstaller
      
      - name: Build macOS app
        run: |
          cd build_tools && python -m PyInstaller --clean --distpath .. --workpath build SilhouetteCardMaker.spec
      
      - name: Get version
        id: get_version
        run: |
          if [[ $GITHUB_REF == refs/tags/* ]]; then
            echo "version=${GITHUB_REF#refs/tags/}" >> $GITHUB_OUTPUT
          else
            echo "version=dev-$(date +'%Y%m%d')" >> $GITHUB_OUTPUT
          fi
      
      - name: Package macOS release
        run: |
          mkdir -p release
          cp -r SilhouetteCardMaker.app release/
          cp -r assets calibration cutting_templates plugins game examples release/
          cp README.md LICENSE.md requirements.txt release/
          cd release
          zip -r ../SilhouetteCardMaker-${{ steps.get_version.outputs.version }}-macOS.zip *
      
      - name: Upload macOS artifact
        uses: actions/upload-artifact@v4
        with:
          name: SilhouetteCardMaker-macOS
          path: SilhouetteCardMaker-${{ steps.get_version.outputs.version }}-macOS.zip

  build-linux:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pyinstaller
      
      - name: Build Linux executable
        run: |
          cd build_tools && python -m PyInstaller --clean --distpath .. --workpath build SilhouetteCardMaker.spec
      
      - name: Get version
        id: get_version
        run: |
          if [[ $GITHUB_REF == refs/tags/* ]]; then
            echo "version=${GITHUB_REF#refs/tags/}" >> $GITHUB_OUTPUT
          else
            echo "version=dev-$(date +'%Y%m%d')" >> $GITHUB_OUTPUT
          fi
      
      - name: Package Linux release
        run: |
          mkdir -p release
          cp SilhouetteCardMaker release/
          cp -r assets calibration cutting_templates plugins game examples release/
          cp README.md LICENSE.md requirements.txt release/
          cd release
          tar -czf ../SilhouetteCardMaker-${{ steps.get_version.outputs.version }}-Linux.tar.gz *
      
      - name: Upload Linux artifact
        uses: actions/upload-artifact@v4
        with:
          name: SilhouetteCardMaker-Linux
          path: SilhouetteCardMaker-${{ steps.get_version.outputs.version }}-Linux.tar.gz

  create-release:
    needs: [build-windows, build-macos, build-linux]
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/')
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
      
      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            SilhouetteCardMaker-Windows/*
            SilhouetteCardMaker-macOS/*
            SilhouetteCardMaker-Linux/*
          draft: false
          prerelease: false
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
